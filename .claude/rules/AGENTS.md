---
trigger: always_on
---

# User Guidelines

- Always use yarn instead of npm.
- REMEMBER always use the MCP tool augment context engine mcp or/and gitnexus mcp (you can load skill /gitnexus-* if you need and remember always use in git repo are cuckhqs and cuckhqs_fe) for indexing the codebase or searching code.
- REMEMBER always use the MCP tool sequential-thinking for thinking.
- Use all MCP tools whenever they might be needed (I recommend using many).
- Do not run terminal commands by yourself; instead, guide me with the commands so I can run them myself (only when I explicitly say “you can run terminal” then you allowed to execute terminal commands).
- When asked about the codebase, project structure, or to find code, always use the augment-context-engine MCP tool (codebase-retrieval) in the root workspace first before reading individual files.
